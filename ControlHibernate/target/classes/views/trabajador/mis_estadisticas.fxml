<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.chart.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="org.example.controller.trabajador.MisEstadisticasController"
            prefHeight="700.0" prefWidth="900.0"
            style="-fx-background-color: #ecf0f1;">

    <!-- HEADER -->
    <top>
        <VBox spacing="10" style="-fx-background-color: #2c3e50; -fx-padding: 20;">
            <HBox alignment="CENTER_LEFT" spacing="20">
                <Label text="📊 Mis Estadísticas" textFill="white">
                    <font>
                        <Font name="System Bold" size="24"/>
                    </font>
                </Label>
            </HBox>

            <!-- FILTROS -->
            <HBox spacing="15" alignment="CENTER_LEFT" style="-fx-padding: 10 0 0 0;">
                <Label text="Periodo:" textFill="white" style="-fx-font-size: 14px; -fx-font-weight: bold;"/>
                <ComboBox fx:id="cbPeriodo" prefWidth="120" promptText="Selecciona"/>

                <Label text="Fecha:" textFill="white" style="-fx-font-size: 14px; -fx-font-weight: bold;"/>
                <DatePicker fx:id="dpFecha" prefWidth="150"/>

                <Button fx:id="btnBuscar" text="🔍 Buscar" onAction="#handleBuscar"
                        style="-fx-background-color: #3498db; -fx-text-fill: white; -fx-font-weight: bold;"/>

                <Region HBox.hgrow="ALWAYS"/>

                <Button fx:id="btnVolver" text="← Volver" onAction="#handleVolver"
                        style="-fx-background-color: #95a5a6; -fx-text-fill: white; -fx-font-weight: bold;"/>
            </HBox>
        </VBox>
    </top>

    <!-- CONTENIDO PRINCIPAL -->
    <center>
        <ScrollPane fitToWidth="true" style="-fx-background-color: transparent;">
            <VBox spacing="15" style="-fx-padding: 20;">

                <!-- TARJETAS DE RESUMEN -->
                <HBox spacing="15" alignment="CENTER">
                    <!-- Tarjeta 1: Total Horas -->
                    <VBox alignment="CENTER" spacing="5" prefWidth="200" prefHeight="100"
                          style="-fx-background-color: white; -fx-background-radius: 10; -fx-padding: 15; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 3);">
                        <Label text="⏱️ Total Horas" style="-fx-font-size: 14px; -fx-text-fill: #7f8c8d;"/>
                        <Label fx:id="lblTotalHoras" text="0.00 h" style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
                    </VBox>

                    <!-- Tarjeta 2: Promedio Diario -->
                    <VBox alignment="CENTER" spacing="5" prefWidth="200" prefHeight="100"
                          style="-fx-background-color: white; -fx-background-radius: 10; -fx-padding: 15; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 3);">
                        <Label text="📊 Promedio Diario" style="-fx-font-size: 14px; -fx-text-fill: #7f8c8d;"/>
                        <Label fx:id="lblPromedio" text="0.00 h" style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: #3498db;"/>
                    </VBox>

                    <!-- Tarjeta 3: Días Trabajados -->
                    <VBox alignment="CENTER" spacing="5" prefWidth="200" prefHeight="100"
                          style="-fx-background-color: white; -fx-background-radius: 10; -fx-padding: 15; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 3);">
                        <Label text="📅 Días Trabajados" style="-fx-font-size: 14px; -fx-text-fill: #7f8c8d;"/>
                        <Label fx:id="lblDiasTrabajados" text="0" style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: #27ae60;"/>
                    </VBox>

                    <!-- Tarjeta 4: Día Máximo -->
                    <VBox alignment="CENTER" spacing="5" prefWidth="200" prefHeight="100"
                          style="-fx-background-color: white; -fx-background-radius: 10; -fx-padding: 15; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 3);">
                        <Label text="🔥 Día Máximo" style="-fx-font-size: 14px; -fx-text-fill: #7f8c8d;"/>
                        <Label fx:id="lblDiaMaximo" text="0.00 h" style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: #e74c3c;"/>
                    </VBox>
                </HBox>

                <!-- GRÁFICOS -->
                <HBox spacing="15">
                    <!-- Gráfico de Barras: Horas por Día -->
                    <VBox spacing="10" HBox.hgrow="ALWAYS"
                          style="-fx-background-color: white; -fx-background-radius: 10; -fx-padding: 15; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 3);">
                        <Label text="📈 Evolución Diaria" style="-fx-font-size: 16px; -fx-font-weight: bold;"/>
                        <BarChart fx:id="chartBarras" prefHeight="300" legendVisible="false">
                            <xAxis>
                                <CategoryAxis label="Fecha" side="BOTTOM"/>
                            </xAxis>
                            <yAxis>
                                <NumberAxis label="Horas" side="LEFT" autoRanging="true"/>
                            </yAxis>
                        </BarChart>
                    </VBox>

                    <!-- Gráfico Circular: Distribución por Día de la Semana -->
                    <VBox spacing="10" prefWidth="350"
                          style="-fx-background-color: white; -fx-background-radius: 10; -fx-padding: 15; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 3);">
                        <Label text="🍕 Distribución Semanal" style="-fx-font-size: 16px; -fx-font-weight: bold;"/>
                        <PieChart fx:id="chartPie" prefHeight="300" legendSide="BOTTOM"/>
                    </VBox>
                </HBox>

                <!-- TABLA DETALLADA -->
                <VBox spacing="10"
                      style="-fx-background-color: white; -fx-background-radius: 10; -fx-padding: 15; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 3);">
                    <Label text="📋 Detalle por Día" style="-fx-font-size: 16px; -fx-font-weight: bold;"/>

                    <TableView fx:id="tableEstadisticas" prefHeight="250">
                        <columns>
                            <TableColumn fx:id="colFecha" text="Fecha" prefWidth="120"/>
                            <TableColumn fx:id="colDiaSemana" text="Día" prefWidth="100"/>
                            <TableColumn fx:id="colEntrada" text="Primera Entrada" prefWidth="120"/>
                            <TableColumn fx:id="colSalida" text="Última Salida" prefWidth="120"/>
                            <TableColumn fx:id="colHoras" text="Total Horas" prefWidth="100"/>
                            <TableColumn fx:id="colClima" text="Clima" prefWidth="120"/>
                            <TableColumn fx:id="colEstado" text="Estado" prefWidth="120"/>
                        </columns>
                    </TableView>
                </VBox>

            </VBox>
        </ScrollPane>
    </center>

</BorderPane>