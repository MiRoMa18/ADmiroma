<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="org.example.controller.FicharController"
            style="-fx-background-color: #ecf0f1;">

    <top>
        <VBox spacing="10" style="-fx-background-color: #34495e; -fx-padding: 20;">
            <Label text="🕒 Fichar Entrada / Salida"
                   style="-fx-font-size: 22px; -fx-text-fill: white; -fx-font-weight: bold;"/>
            <Label fx:id="lblNombreTrabajador" text="Trabajador" 
                   style="-fx-text-fill: white; -fx-font-size: 16px;"/>
        </VBox>
    </top>

    <center>
        <VBox spacing="20" alignment="CENTER" style="-fx-padding: 30;">
            
            <!-- Tipo de acción (ENTRADA/SALIDA) -->
            <VBox spacing="10" alignment="CENTER" 
                  style="-fx-background-color: white; -fx-padding: 20; -fx-background-radius: 10; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 2);">
                <Label text="Próxima acción:" style="-fx-font-size: 14px; -fx-text-fill: #7f8c8d;"/>
                <Label fx:id="lblTipoAccion" text="🟢 ENTRADA" 
                       style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: #27ae60;"/>
            </VBox>

            <!-- Fecha y Hora actual (solo lectura) -->
            <VBox spacing="8" alignment="CENTER"
                  style="-fx-background-color: white; -fx-padding: 20; -fx-background-radius: 10; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 2);">
                <Label text="Se registrará:" style="-fx-font-size: 14px; -fx-text-fill: #7f8c8d;"/>
                <Label fx:id="lblFechaHoraActual" text="Sábado, 08 de Diciembre de 2025 - 11:30:45" 
                       style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
            </VBox>

            <!-- Clima -->
            <VBox spacing="8" alignment="CENTER">
                <Label fx:id="lblClima" text="🌤️ Clima: Cargando..." 
                       style="-fx-font-size: 14px; -fx-text-fill: #2980b9; -fx-font-weight: bold;"/>
            </VBox>

            <!-- Notas -->
            <VBox spacing="8" style="-fx-background-color: white; -fx-padding: 15; -fx-background-radius: 8; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.08), 8, 0, 0, 2); -fx-max-width: 500;">
                <Label text="Notas (opcional)" style="-fx-font-size: 14px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
                <TextArea fx:id="txtNotas" prefRowCount="4" wrapText="true" 
                          promptText="Añade una nota para este fichaje..."
                          style="-fx-background-color: #f8f9fa; -fx-border-color: #dee2e6; -fx-border-radius: 4;"/>
            </VBox>

            <!-- Botones -->
            <HBox spacing="15" alignment="CENTER" style="-fx-padding: 20 0 0 0;">
                <Button fx:id="btnFichar" text="✅ Fichar" onAction="#handleFichar"
                        prefWidth="150"
                        styleClass="btn-entrada"
                        style="-fx-background-color: #27ae60; -fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 16px; -fx-padding: 12 24; -fx-background-radius: 6; -fx-cursor: hand;"/>
                <Button fx:id="btnVolver" text="⬅️ Volver" onAction="#handleVolver"
                        prefWidth="150"
                        style="-fx-background-color: #95a5a6; -fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 16px; -fx-padding: 12 24; -fx-background-radius: 6; -fx-cursor: hand;"/>
            </HBox>
        </VBox>
    </center>

</BorderPane>
