<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Configuración de la conexión a la base de datos -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/control_horario</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">RootPass123!</property>

        <!-- Dialecto SQL para MySQL -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Mostrar SQL en consola -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Pool de conexiones -->
        <property name="hibernate.connection.pool_size">10</property>

        <!-- Política de actualización del esquema -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Contexto de sesión actual -->
        <property name="hibernate.current_session_context_class">thread</property>

        <!-- MAPEO DE ENTIDADES - Añade aquí tus clases -->
        <mapping class="org.example.model.Trabajador"/>
        <mapping class="org.example.model.Fichaje"/>
    </session-factory>
</hibernate-configuration>