<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="org.example.controller.DashboardController"
            style="-fx-background-color: #ecf0f1;">

    <!-- Header -->
    <top>
        <VBox alignment="CENTER" spacing="5" style="-fx-background-color: #2c3e50; -fx-padding: 20;">
            <Label fx:id="lblBienvenida"
                   text="¡Bienvenido!"
                   style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: white;"/>
            <Label fx:id="lblRol"
                   text="Rol: TRABAJADOR"
                   style="-fx-font-size: 14px; -fx-text-fill: #ecf0f1;"/>
        </VBox>
    </top>

    <!-- Menú Principal -->
    <center>
        <ScrollPane fitToWidth="true" style="-fx-background-color: transparent;">
            <VBox alignment="TOP_CENTER" spacing="20" style="-fx-padding: 30;">

                <!-- MENÚ SOLO ADMIN -->
                <VBox fx:id="menuAdmin" spacing="15" alignment="CENTER">
                    <Label text="═══ Panel de Administrador ═══"
                           style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #e74c3c;"/>

                    <Button fx:id="btnVerTodosFichajes"
                            text="📋 Ver Todos los Fichajes (CRUD)"
                            onAction="#handleVerTodosFichajes"
                            prefWidth="350"
                            style="-fx-background-color: #3498db; -fx-text-fill: white; -fx-font-size: 16px; -fx-padding: 15; -fx-cursor: hand;"/>

                    <Button fx:id="btnEstadisticasGlobales"
                            text="📊 Estadísticas Globales"
                            onAction="#handleEstadisticasGlobales"
                            prefWidth="350"
                            style="-fx-background-color: #9b59b6; -fx-text-fill: white; -fx-font-size: 16px; -fx-padding: 15; -fx-cursor: hand;"/>

                    <Button fx:id="btnCrudTrabajadores"
                            text="👥 Gestionar Trabajadores (CRUD)"
                            onAction="#handleCrudTrabajadores"
                            prefWidth="350"
                            style="-fx-background-color: #e67e22; -fx-text-fill: white; -fx-font-size: 16px; -fx-padding: 15; -fx-cursor: hand;"/>

                    <Separator prefWidth="350"/>
                </VBox>

                <!-- MENÚ COMÚN (Admin y Trabajador) -->
                <VBox fx:id="menuComun" spacing="15" alignment="CENTER">
                    <Label text="═══ Mi Panel Personal ═══"
                           style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #27ae60;"/>

                    <Button fx:id="btnMisFichajes"
                            text="📅 Mis Fichajes"
                            onAction="#handleMisFichajes"
                            prefWidth="350"
                            style="-fx-background-color: #1abc9c; -fx-text-fill: white; -fx-font-size: 16px; -fx-padding: 15; -fx-cursor: hand;"/>

                    <Button fx:id="btnMisEstadisticas"
                            text="📈 Mis Estadísticas"
                            onAction="#handleMisEstadisticas"
                            prefWidth="350"
                            style="-fx-background-color: #16a085; -fx-text-fill: white; -fx-font-size: 16px; -fx-padding: 15; -fx-cursor: hand;"/>

                    <Button fx:id="btnMisDatos"
                            text="🆔 Mi Tarjeta y PIN"
                            onAction="#handleMisDatos"
                            prefWidth="350"
                            style="-fx-background-color: #34495e; -fx-text-fill: white; -fx-font-size: 16px; -fx-padding: 15; -fx-cursor: hand;"/>

                    <Button fx:id="btnFichar"
                            text="⏱️ Fichar Entrada/Salida"
                            onAction="#handleFichar"
                            prefWidth="350"
                            style="-fx-background-color: #27ae60; -fx-text-fill: white; -fx-font-size: 18px; -fx-font-weight: bold; -fx-padding: 20; -fx-cursor: hand;"/>
                </VBox>

            </VBox>
        </ScrollPane>
    </center>

    <!-- Footer con botón cerrar sesión -->
    <bottom>
        <HBox alignment="CENTER" style="-fx-background-color: #34495e; -fx-padding: 15;">
            <Button fx:id="btnCerrarSesion"
                    text="🚪 Cerrar Sesión"
                    onAction="#handleCerrarSesion"
                    style="-fx-background-color: #c0392b; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 30; -fx-cursor: hand;"/>
        </HBox>
    </bottom>

</BorderPane>