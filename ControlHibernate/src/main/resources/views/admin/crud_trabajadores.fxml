<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="org.example.controller.admin.CrudTrabajadoresController"
            style="-fx-background-color: #ecf0f1;">

    <!-- HEADER -->
    <top>
        <VBox spacing="15" style="-fx-background-color: #34495e; -fx-padding: 20;">
            
            <!-- Título -->
            <Label text="👥 Gestión de Trabajadores - CRUD Administrador"
                   style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: white;"/>

            <!-- Filtros -->
            <HBox spacing="15" alignment="CENTER_LEFT">
                <Label text="Buscar:" style="-fx-text-fill: white; -fx-font-size: 14px; -fx-font-weight: bold;"/>
                <TextField fx:id="txtBuscar" 
                           promptText="Nombre, tarjeta o email..."
                           prefWidth="300"
                           style="-fx-font-size: 14px;"/>

                <Label text="Rol:" style="-fx-text-fill: white; -fx-font-size: 14px; -fx-font-weight: bold;"/>
                <ComboBox fx:id="cbRol" prefWidth="150" style="-fx-font-size: 14px;"/>

                <Button fx:id="btnBuscar"
                        text="🔍 Buscar"
                        onAction="#handleBuscar"
                        style="-fx-background-color: #3498db; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 8 20; -fx-cursor: hand; -fx-font-weight: bold;"/>

                <Button fx:id="btnLimpiar"
                        text="🧹 Limpiar"
                        onAction="#handleLimpiar"
                        style="-fx-background-color: #95a5a6; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 8 20; -fx-cursor: hand;"/>

                <Region HBox.hgrow="ALWAYS"/>

                <Button fx:id="btnVolver"
                        text="⬅️ Volver"
                        onAction="#handleVolver"
                        style="-fx-background-color: #7f8c8d; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 8 20; -fx-cursor: hand; -fx-font-weight: bold;"/>
            </HBox>
        </VBox>
    </top>

    <!-- CONTENIDO PRINCIPAL -->
    <center>
        <VBox spacing="15" style="-fx-padding: 20;">

            <!-- Toolbar con botones de acción -->
            <HBox spacing="15" alignment="CENTER_LEFT" style="-fx-background-color: white; -fx-padding: 15; -fx-background-radius: 10; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 3);">
                
                <Label text="ACCIONES:" style="-fx-font-size: 13px; -fx-font-weight: bold; -fx-text-fill: #7f8c8d;"/>
                
                <Button fx:id="btnNuevo"
                        text="➕ Nuevo Trabajador"
                        onAction="#handleNuevo"
                        style="-fx-background-color: #27ae60; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 20; -fx-cursor: hand; -fx-font-weight: bold;"/>

                <Button fx:id="btnEditar"
                        text="✏️ Editar"
                        onAction="#handleEditar"
                        style="-fx-background-color: #f39c12; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 20; -fx-cursor: hand; -fx-font-weight: bold;"/>

                <Button fx:id="btnEliminar"
                        text="🗑️ Eliminar"
                        onAction="#handleEliminar"
                        style="-fx-background-color: #e74c3c; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 20; -fx-cursor: hand; -fx-font-weight: bold;"/>

                <Separator orientation="VERTICAL"/>

                <Button fx:id="btnRefrescar"
                        text="🔄 Refrescar"
                        onAction="#handleRefrescar"
                        style="-fx-background-color: #3498db; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 20; -fx-cursor: hand;"/>

                <Region HBox.hgrow="ALWAYS"/>

                <Label fx:id="lblContador" 
                       text="Mostrando 0 trabajador(es)"
                       style="-fx-font-size: 13px; -fx-text-fill: #7f8c8d; -fx-font-weight: bold;"/>
            </HBox>

            <!-- Tabla de trabajadores -->
            <VBox style="-fx-background-color: white; -fx-background-radius: 10; -fx-padding: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 3);">
                
                <Label text="📋 Lista de Trabajadores" 
                       style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #2c3e50; -fx-padding: 0 0 15 0;"/>
                
                <TableView fx:id="tableTrabajadores" VBox.vgrow="ALWAYS" prefHeight="500">
                    <columns>
                        <TableColumn fx:id="colId" text="ID" prefWidth="50" minWidth="40"/>
                        <TableColumn fx:id="colNumeroTarjeta" text="Nº Tarjeta" prefWidth="100" minWidth="80"/>
                        <TableColumn fx:id="colNombre" text="Nombre" prefWidth="140" minWidth="100"/>
                        <TableColumn fx:id="colApellidos" text="Apellidos" prefWidth="160" minWidth="120"/>
                        <TableColumn fx:id="colEmail" text="Email" prefWidth="200" minWidth="150"/>
                        <TableColumn fx:id="colRol" text="Rol" prefWidth="110" minWidth="90"/>
                        <TableColumn fx:id="colFechaAlta" text="Fecha Alta" prefWidth="110" minWidth="90"/>
                    </columns>
                    <columnResizePolicy>
                        <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
                    </columnResizePolicy>
                </TableView>

                <!-- Nota informativa -->
                <Label text="💡 Haz doble clic en una fila para editar rápidamente" 
                       style="-fx-font-size: 11px; -fx-text-fill: #7f8c8d; -fx-font-style: italic; -fx-padding: 10 0 0 0;"/>
            </VBox>

        </VBox>
    </center>

</BorderPane>
